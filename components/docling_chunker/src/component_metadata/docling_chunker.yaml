# PIPELINE DEFINITION
# Name: docling-chunker
# Inputs:
#    inut_dir: str
#    output_dir: str
# Outputs:
#    Output: str
components:
  comp-docling-chunker:
    executorLabel: exec-docling-chunker
    inputDefinitions:
      parameters:
        inut_dir:
          parameterType: STRING
        output_dir:
          description: Directory to save the converted documents.
          parameterType: STRING
    outputDefinitions:
      parameters:
        Output:
          parameterType: STRING
deploymentSpec:
  executors:
    exec-docling-chunker:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - docling_chunker
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'docling[vlm]==2.31.0'\
          \ 'load_dotenv==0.1.0' && \"$0\" \"$@\"\n"
        - python3
        - -m
        - kfp.dsl.executor_main
        image: quay.io/atarazana/docling_chunker:v1.0.1
pipelineInfo:
  name: docling-chunker
root:
  dag:
    outputs:
      parameters:
        Output:
          valueFromParameter:
            outputParameterKey: Output
            producerSubtask: docling-chunker
    tasks:
      docling-chunker:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-docling-chunker
        inputs:
          parameters:
            inut_dir:
              componentInputParameter: inut_dir
            output_dir:
              componentInputParameter: output_dir
        taskInfo:
          name: docling-chunker
  inputDefinitions:
    parameters:
      inut_dir:
        parameterType: STRING
      output_dir:
        description: Directory to save the converted documents.
        parameterType: STRING
  outputDefinitions:
    parameters:
      Output:
        parameterType: STRING
schemaVersion: 2.1.0
sdkVersion: kfp-2.10.1
